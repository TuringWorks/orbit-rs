Replace all function signatures that say:
) -> ProtocolResult<impl IntoResponse> {

With:
) -> (StatusCode, Json<SuccessResponse<ActorInfo>>) {

And remove the "Ok(" and final ")" from each return statement.

For list_actors specifically, change to:
) -> (StatusCode, Json<SuccessResponse<PagedResponse<ActorInfo>>>) {
